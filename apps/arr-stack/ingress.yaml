---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: arr-stack
  namespace: homelab
spec:
  ingressClassName: nginx
  rules:
    - host: jellyfin.home.rizexor.com
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: arr-stack
              port:
                number: 80
  tls:
  - secretName: homelab
