---
apiVersion: v1
kind: Service
metadata:
  name: arr-stack
  namespace: homelab
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8096
      protocol: TCP
  selector:
    app: arr-stack

